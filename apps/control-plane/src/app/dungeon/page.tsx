import { Dungeon } from "@/components/dungeon";

export const metadata = {
  title: "The Dungeon — StandUp",
  description:
    "Three models, three cells. Where they agree the agent acts; where they disagree a human decides.",
};

export default function Page() {
  return <Dungeon />;
}
